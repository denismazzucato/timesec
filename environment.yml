name: quantit
channels:
  - defaults
  - conda-forge
dependencies:
  - python >=3.12
  - numpy
  - scipy
  - colorlog
  - pycparser
  - frozendict
  - tqdm
  - matplotlib
  - tabulate
  - pip
  - pip:
    - typing-extensions
    - pynput
    - pydot
  # - pip:
  #   - git+https://github.com/caterinaurban/apronpy.git@refs/pull/7/head
